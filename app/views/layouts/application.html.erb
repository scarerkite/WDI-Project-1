<!DOCTYPE html>
<html>
<head>
  <title>Game App</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="header">
    <% if current_user %>
      Hello, <%= current_user.user_name %>
      <%= link_to "logout", session_path(session), method: :delete, confirm: "Are you sure you want to log out?", :class => 'right_button' %>
      <%= link_to "Overview", overview_user_path(current_user), :class => 'left_button' %>
      <%= link_to "Opponents", opponents_user_path(current_user), :class => 'left_button' %>
    <% else %>
      <%= link_to "Login", login_path, :class => 'button' %>
      <%= link_to "Sign up", new_user_path, :class => 'button' %>
    <% end %>
  <% flash.each do |name, message| %>
    <div class="flash-message flash-message-<%= name %>">
      <%= message %>
  <% end %>

    </div>
    
  </div>
  </div>
  <div>
  <%= yield %>
  </div>
  <div class="footer">
    <h1>Friendly Noughts and Crosses</h1>
  </div>
</body>

</html>
